namespace IDontCare.Constants
{
    public enum FilterMode : int
    {
        Default = 0,
        FilterAll = 1,
        FilterNothing = 2,
        OnlyMe = 3,
        OnlyMyClan = 4,
        OnlyMyKingdom = 5
    }

    public enum InformationType : int
    {
        LogEntry = 0,
        Notification = 1
    }
}
